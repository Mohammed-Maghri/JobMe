/**
 * One page gutter for every section, so the hero, the cards, the pipeline and
 * the footer all sit on the same vertical rails.
 */
export const CONTAINER = "mx-auto w-full max-w-[1360px] px-5 sm:px-8 lg:px-10";
